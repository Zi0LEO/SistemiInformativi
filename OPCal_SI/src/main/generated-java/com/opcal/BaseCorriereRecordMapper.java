package com.opcal;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import org.apache.torque.om.mapper.RecordMapper;


/**
 * Maps ResultSet rows into Corriere objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Thu Mar 13 14:45:23 CET 2025]
 *
 */
@SuppressWarnings("unused")
public class BaseCorriereRecordMapper implements RecordMapper<Corriere>
{
    /** Serial version */
    private static final long serialVersionUID = 1741873523134L;

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseCorriereRecordMapper.class);

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param offset a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the corriere table.
     */
    public Corriere processRow(
                ResultSet resultSet,
                int offset,
                Criteria criteria)
            throws TorqueException
    {
        Corriere corriere = new Corriere();

        try 
        {
            corriere.setLoading(true);
            if (criteria == null)
            {
                corriere.setIva(
                        getIva(resultSet, offset + 1));
                corriere.setNome(
                        getNome(resultSet, offset + 2));
                corriere.setSito(
                        getSito(resultSet, offset + 3));
                corriere.setTelefono(
                        getTelefono(resultSet, offset + 4));
                corriere.setPrezzo1(
                        getPrezzo1(resultSet, offset + 5));
                corriere.setPrezzo10(
                        getPrezzo10(resultSet, offset + 6));
                corriere.setPrezzo100(
                        getPrezzo100(resultSet, offset + 7));
            }
            else
            {
                // try to get columns to be mapped
                // from criteria's select columns
                boolean columnMapped = false;
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                for (Column column : columnsWithoutOffset)
                {
                    if (BaseCorrierePeer.IVA.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setIva(
                            getIva(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.NOME.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setNome(
                            getNome(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.SITO.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setSito(
                            getSito(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.TELEFONO.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setTelefono(
                            getTelefono(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.PREZZO1.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setPrezzo1(
                            getPrezzo1(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.PREZZO10.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setPrezzo10(
                            getPrezzo10(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseCorrierePeer.PREZZO100.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        corriere.setPrezzo100(
                            getPrezzo100(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    totalOffset++;
                }
                if (!columnMapped)
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
            }
            corriere.setNew(false);
            corriere.setModified(false);
        }
        finally
        {
            corriere.setLoading(false);
        }
        return corriere;
    }

    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field iva in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getIva(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field nome in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getNome(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field sito in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getSito(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field telefono in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getTelefono(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field prezzo1 in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getPrezzo1(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field prezzo10 in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getPrezzo10(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field prezzo100 in Corriere.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getPrezzo100(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }

}
